# RedBut Web App Environment Variables
# Ensures the Next.js frontend runs on port 3000

# Server configuration
PORT=3003

# API endpoints
# Base URL where the NestJS backend is listening
NEXT_PUBLIC_API_URL=http://localhost:3001
# (Optional) internal usage for server-side fetches if needed
API_URL=http://localhost:3001

# WebSocket URL for real-time chat
NEXT_PUBLIC_WS_URL=http://localhost:3001/chat

# Explicitly expose the frontend URL (useful for CORS and redirects)
FRONTEND_URL=http://localhost:3003

# Feature flags
NEXT_PUBLIC_ENABLE_CHAT_HISTORY=true

# Application settings
NEXT_PUBLIC_APP_NAME=RedBut
NEXT_PUBLIC_APP_DESCRIPTION=Restaurant Waiter Assistant Application
